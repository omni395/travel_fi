<%# lookbook/ui/btn_component_preview/all_sizes.html.erb %>
<div class="flex items-center gap-3 flex-wrap">
  <% sizes.each do |size| %>
    <%= render Ui::BtnComponent.new(color: color, size: size) do %>
      Button <%= size.to_s.upcase %>
    <% end %>
  <% end %>
</div>
