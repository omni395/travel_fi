<%# frozen_string_literal: true %>

<%#
# app/views/admin/poi_categories/new.html.erb - создание новой категории POI
#
# Отображает:
# - Хлебные крошки
# - Форму создания категории (EditComponent с пустой категорией)
# %>
<div class="max-w-4xl mx-auto">
  <%= render Ui::BreadcrumbsComponent.new(breadcrumbs: [
    { name: t("admin.poi_categories.index.title"), path: admin_poi_categories_path },
    { name: t("admin.poi_categories.new.title") }
  ]) %>

  <%= render Admin::PoiCategories::PoiCategory::EditComponent.new(category: PoiCategory.new) %>
</div>
