<%# frozen_string_literal: true %>

<% content_for :title, t("admin.dashboard.title") %>

<%# Хлебные крошки %>
<%= render Ui::BreadcrumbsComponent.new(breadcrumbs: [
  { name: t("admin.dashboard.title") }
]) %>

<%# Дашборд через Admin::DashboardComponent (включает статистику + пользователи + аудит) %>
<%= render Admin::DashboardComponent.new(
  stats: @stats,
  recent_users: @recent_users,
  recent_activities: @recent_activities
) %>
