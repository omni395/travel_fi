import { Controller } from "@hotwired/stimulus"

// Admin::PoiCategory::TableComponent — пассивный контроллер таблицы категорий
export default class extends Controller {
  connect() {
    // Пассивный контроллер — без активной логики
  }
}
