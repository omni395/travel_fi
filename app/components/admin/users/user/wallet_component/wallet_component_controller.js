// Admin::Users::User::WalletComponent
// Stimulus controller for wallet placeholder
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // Passive component — no interactive logic needed
  }
}
