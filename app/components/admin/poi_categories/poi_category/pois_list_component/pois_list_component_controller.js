import { Controller } from "@hotwired/stimulus"

// Admin::PoiCategories::PoiCategory::PoisListComponent — пассивный контроллер
export default class extends Controller {
  connect() {
    // Пассивный контроллер
  }
}
