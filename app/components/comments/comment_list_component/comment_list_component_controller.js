import { Controller } from '@hotwired/stimulus'

/**
 * Comments::CommentListComponent — контейнер списка комментариев.
 * Без интерактивности: используется как цель точечного broadcast.
 */
export default class extends Controller {}
