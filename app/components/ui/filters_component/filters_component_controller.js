// Ui::FiltersComponent — пассивный компонент
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // Пассивный компонент — логика фильтрации в родительском контроллере
  }
}
