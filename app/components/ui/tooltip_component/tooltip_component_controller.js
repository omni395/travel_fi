import { Controller } from "@hotwired/stimulus"

// Контроллер для UI::TooltipComponent
// Иконка: mdi-tooltip
//
// Пассивный компонент — управляется через DOM из map_component_controller.js
// Контроллер создан для соблюдения архитектурного паттерна sidecar.
export default class extends Controller {
  connect() {
    // Управляется через map_component_controller.js
  }
}
