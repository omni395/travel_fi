import ApplicationController from '../../../javascript/controllers/application_controller'

/**
 * Poi::GalleryComponent Controller
 * Иконка: mdi-image-multiple-outline
 *
 * ЗАГЛУШКА — контроллер пуст. Реализация галереи (сетка + lightbox/слайдер)
 * запланирована в ROADMAP. Данные Poi#photos уже доступны через PhotoService.
 */
export default class extends ApplicationController {
  connect() {
    super.connect()
  }
}
