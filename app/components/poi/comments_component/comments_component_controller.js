import ApplicationController from '../../../javascript/controllers/application_controller'

/**
 * Poi::CommentsComponent Controller
 * Иконка: mdi-comment-text-outline
 *
 * ЗАГЛУШКА — контроллер пуст. Реализация комментариев (live, антифрод)
 * запланирована в ROADMAP и будет добавлена вместе с логикой.
 */
export default class extends ApplicationController {
  connect() {
    super.connect()
  }
}
